export enum BallDirection {
    UP = 1,
    DOWN,
    L_UP,
    L_DOWN,
    R_UP,
    R_DOWN
}

export enum PaddleSize {
    WIDTH = 2,
    HEIGHT = 10,
    DEPTH = 50
}

export enum VerticalWallSize {
    WIDTH = 90,
    HEIGHT = 10,
    DEPTH = 2
}

export enum HorizontalWallSize {
    WIDTH = 5,
    HEIGHT = 15,
    DEPTH = 150
}

export enum PaddleDirection {
    LEFT,
    RIGHT,
    NONE
}
